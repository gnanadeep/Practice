// Expressions;

// 1)write code add, subtract, multiplication,division two numbers using variables.
var a ,b
var add = a+b;
var sub = a-b;
var mul = a*b;
var div = a/b;

// 2) pre and post increment and decrement of variables short hand notations.
var i ;
++i; //pre 
 i++; //post
// 3) Logical and / or / not operations using variables <, >, <=, >= , ==, ===  conditions.
